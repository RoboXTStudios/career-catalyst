# Ground Control

Sprint 1: Signal Acquired.

Ground Control is a separate local-first Streamlit app that can run beside Career Catalyst without touching the existing Career Catalyst cockpit.

Run it from the repository root:

```bash
streamlit run ground_control/app.py
```

Edit the starting values in `ground_control/seed_data.py`.
