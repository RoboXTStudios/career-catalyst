# Tests

This folder is reserved for future tests.

Sprint 0 does not include application logic, so there are no test files yet. Future tests should validate YAML loading, required fields, parsing behavior, scoring behavior, generation behavior, and export behavior as those features are introduced.
