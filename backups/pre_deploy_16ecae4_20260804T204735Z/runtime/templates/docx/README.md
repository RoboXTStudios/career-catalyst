# DOCX Templates

Place `styled_resume_template.docx` in this folder to provide the visual style reference for styled resume exports. Styled exports use the template when it is present and fall back to the built-in professional formatting when it is not.

ATS exports intentionally ignore template styling and generate a plain, single-column resume with no tables.
